# ── Supabase ───────────────────────────────────────────────────────────
# Project Settings > API no painel do Supabase
NEXT_PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key-aqui
# Necessária para o cron de lembretes automáticos (acesso admin ao banco)
# Supabase → Project Settings → API → "service_role" key (NUNCA expor no frontend)
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# ── URL pública ─────────────────────────────────────────────────────────
# Em produção: URL da Vercel  |  Em dev: http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── E-mail (opcional) ───────────────────────────────────────────────────
# Resend: https://resend.com (100 e-mails/dia grátis)
# Se não definida, as notificações por e-mail são silenciosamente ignoradas.
RESEND_API_KEY=re_xxxxxxxxxxxx
# Se não tiver domínio verificado no Resend, use onboarding@resend.dev (funciona direto)
# Para domínio próprio: verifique o domínio em resend.com/domains e altere abaixo
RESEND_FROM=Agendou <onboarding@resend.dev>
# E-mail que recebe mensagens da Central de Ajuda (fluxo antigo, ainda usado por /api/support)
SUPPORT_EMAIL=andersonnpereira@gmail.com

# ── WhatsApp de suporte ─────────────────────────────────────────────────
# Número que recebe as dúvidas da Central de Ajuda e o contato da página de planos.
# A Central de Ajuda agora abre o WhatsApp deste número com a mensagem pronta.
# Formato: DDI+DDD+número, só dígitos. Ex.: 5567999998888
NEXT_PUBLIC_CONTACT_WHATSAPP=5567999998888

# ── WhatsApp API ────────────────────────────────────────────────────────
# Provedor: mock (padrão dev) | zapi | evolution | ultramsg
WHATSAPP_PROVIDER=mock

# Z-API  →  https://z-api.io
WHATSAPP_INSTANCE_ID=SUA_INSTANCIA
WHATSAPP_TOKEN=SEU_TOKEN
ZAPI_CLIENT_TOKEN=SEU_CLIENT_TOKEN

# Evolution API  →  instância self-hosted
# EVOLUTION_API_URL=https://seu-evolution.dominio.com.br
# WHATSAPP_INSTANCE_ID=nome-da-instancia
# WHATSAPP_TOKEN=global-api-key

# Ultramsg  →  https://ultramsg.com
# WHATSAPP_INSTANCE_ID=instanceXXXX
# WHATSAPP_TOKEN=seu-token
